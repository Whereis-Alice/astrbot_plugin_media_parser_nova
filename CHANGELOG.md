# 更新日志

本项目使用独立版本号；每次 Nova 维护版本的修复和改进都会记录在这里。

## v1.9.1 (2026-08-29)

### 修复：按 README 的步骤导出 Cookie，粘进配置却不生效

上一版让 Cookie 能自动跟进轮换，但 README 推荐的导出工具 `Get cookies.txt LOCALLY` 产出的是 **Netscape 格式的 cookies.txt**（多行、制表符分隔），而配置项此前只认 `a=1; b=2` 形式的 Cookie 请求头。照着文档做的人会把 cookies.txt 整段粘进去，然后插件解析出一堆无意义的键、找不到 `SAPISID`、静默退回匿名请求——日志里只有一句「找不到 SAPISID」，很难联想到是格式问题。文档指着一条走不通的路，这算插件的 bug。

- **`youtube.cookie` 现在三种格式都收**，粘进去自动识别并转换成请求头：
  - `cookies.txt`（Netscape 格式，`Get cookies.txt LOCALLY` 的产物；兼容 `#HttpOnly_` 前缀行、注释行，以及被编辑器把制表符换成空格的情况）
  - 扩展导出的 **JSON 数组**（`Cookie-Editor` / `EditThisCookie` 的默认格式，也兼容外层包一个 `cookies` 键的变体）
  - `a=1; b=2` 形式的 **Cookie 请求头**（原有格式，逐字节原样保留，既有配置不受影响）
- 粘贴时带进来的换行、BOM 和多余空白会被折叠掉，不再需要手动把多行拼成一行
- 归一化发生在配置解析阶段，因此 SAPISIDHASH 鉴权、轮换吸收与落盘指纹全都基于转换后的结果，行为一致

### 文档

- README 导出步骤补上三种可粘格式的说明，并注明 **Edge 加载项商店里没有 `Get cookies.txt LOCALLY`**：可以在 Edge 里打开 Chrome 应用店并允许「来自其他商店的扩展」，或改用商店里的 `Cookie-Editor`
- 配置项 `youtube.cookie` 的面板提示同步说明可粘格式

### 测试

- 新增 12 个用例：请求头原样返回、空值 / 空白 / BOM、多行粘贴折叠、Netscape 三种变体（注释行、`#HttpOnly_` 前缀、空值行、空格代替制表符）、JSON 数组与 `cookies` 包裹变体、JSON 里无名条目跳过、坏 JSON 回退按请求头处理，以及归一化结果能正常驱动 SAPISIDHASH 鉴权
- 全量 354 passed + 82 subtests
## v1.9.0 (2026-08-29)

### 让 YouTube Cookie 从「隔几天就得重导」变成长期免维护

先说清一件容易被误会的事：YouTube Cookie 感觉特别容易失效，**主因不是有效期短**，而是 Google 会在几乎每次请求的响应里用 `Set-Cookie` 轮换 `__Secure-1PSIDTS` / `__Secure-3PSIDTS` / `SIDCC` 这几枚短周期凭据。浏览器会静默跟进，所以你自己刷 YouTube 永远不掉登录；而插件此前每次都原样发配置里那份静态字符串，等于一个「永不更新凭据的浏览器」，服务端迟早判定会话过期。

- **新增 `YouTubeCookieRuntime`（`nova_core/parser/runtime_manager/youtube/cookie.py`）**：每次 Innertube 与 watch 页面请求的响应都会被解析 `Set-Cookie` 并吸收轮换值，下次请求带上最新的那份，行为对齐真实浏览器
- **落盘持久化**：轮换结果写入 `<缓存目录>/runtime_manager/youtube/cookie.json`，重启 AstrBot 不会退回到最初那份旧 Cookie。写入采用临时文件 + 原子替换，权限 `0600`；文件里记了配置 Cookie 全串的 sha256 指纹，一旦你在面板换了新 Cookie，旧状态会被整份丢弃而不是混用
- **主动保鲜**：新增后台任务，默认每 6 小时带当前 Cookie 访问一次 `youtube.com/account`（附 SAPISIDHASH 授权头），既顺手吸收一轮轮换，也能在真正掉登录时第一时间发现。长时间没人解析 YouTube 也不会「放着放着就凉了」
- **4xx 也先吸收再抛错**：Google 在 401/403 响应里同样会下发新的轮换 Cookie，此前 `raise_for_status()` 抢在解析之前执行，等于把唯一的自救机会丢掉了
- **只认白名单、只进不删**：仅吸收身份类与轮换类已知 Cookie 名；服务端下发的删除指令（空值 / `EXPIRED` / `DELETED` / `max-age<=0`）一律忽略，避免一次异常响应就把整罐 Cookie 清空
- **日志只出 Cookie 名，绝不出取值**：状态摘要形如「已吸收 3 枚轮换 Cookie（__Secure-1PSIDTS, __Secure-3PSIDTS, SIDCC），修订 4」，凭据本身不进日志
- 保鲜请求判定为未登录时，除 WARNING 外还会走已有的私聊提醒链（新增 reason 码 `keepalive_logged_out`），提醒文案补上「重新填一次后插件会自动跟进轮换」与「反复出现建议配住宅代理」
- `cookie.json` 已加入 `.gitignore`，不会被误提交

### 新增配置项

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `youtube.cookie_auto_refresh` | 开 | 自动跟进服务端 Cookie 轮换并落盘。关掉则退回旧行为（每次原样发配置里那份） |
| `youtube.cookie_keepalive_hours` | 6 | 主动保鲜间隔（小时），`0` 表示不主动保鲜，上限 168 |

### 文档

- README 的「Cookie 获取与保鲜」整节重写为「**让 Cookie 长期不用再管**」，讲清轮换机制，并给出三层组合拳：冻结会话式导出（无痕窗口 → 登录 → 同标签页跳 `robots.txt` → 本地导出 → 直接关窗口而不点登出）→ 插件自动跟进轮换 → 住宅代理兜底
- 需要强调的现实：**不存在永不过期的 YouTube Cookie**，Google 不发长期令牌。能做到的是「手工导出一次，之后长期不用再管」

### 测试

- 新增 23 个用例：Cookie 轮换吸收（多值 / 单值响应头、白名单过滤、删除指令忽略、修订号递增）、状态文件读写（原子写、权限、指纹隔离、损坏文件容错）、保鲜请求的登录 / 未登录 / 网络异常三条路径、未轮换时逐字节回放原配置串，以及两个新配置项的默认值 / 关闭 / 越界钳制 / 非法值回退
- 全量 342 passed + 78 subtests
## v1.8.3 (2026-08-29)

### 被机器人门禁挡下的 YouTube 视频，卡片不再只剩一句话

上一版把「取不到流」这件事写进了日志，但卡片本身还是残的：门禁会连 `videoDetails` 一起吞掉，于是时长、播放量、点赞全部为空，卡面上只剩一个标题和一句「仅展示封面与信息」。这一版把这些字段全部捞回来了。

- **新增 TVHTML5_SIMPLY 元数据兜底客户端**：实测同一个被门禁拦下的视频，出流客户端（iOS / ANDROID_VR / web / mweb）返回的 `videoDetails` 是空的，而 TVHTML5_SIMPLY 仍然完整下发标题、作者、时长、播放量。现在取不到 `videoDetails` 时会专门跑它一趟补字段，顺带省掉原先那次 1.1MB 的 watch 页面抓取（watch 页面在门禁下也没有 duration / interactionCount 元信息，验证过）
- 这一趟**只借元数据，不动 `playabilityStatus`**：否则「被机器人验证挡下」会退化成含糊的「无法播放」，配套的处理建议也会一起消失
- **修复点赞数在 2026 版页面结构下恒为 0**：`likeCountEntity` 现在是个只剩 `unset_like_count_entity_key` 的空壳，无障碍文案也从 `N likes` 改成了 `like this video along with N other people`，旧的 `toggleButtonRenderer` 则整个下线。现在按「精确整数 → 展开文案 → 压缩文案」四路取值，并限定在点赞按钮的作用域内找，不会误读旁边的「不喜欢」或分享按钮
- **修复门禁下播放量丢失**：`videoDetails` 缺失时改从 `next` 端点的 `videoViewCountRenderer` 兜底（`viewCount` → `originalViewCount` → `shortViewCount`）
- 实测效果（同一个被门禁拦下的视频）：卡片统计行从只有 `💬454` 变成 `👀24万 👍6552 💬454`，时长栏从空白变成 `01:24`，标题与作者不再依赖 oembed

### 修正「时长：被 YouTube 机器人验证挡下」这种读不通的行

- `access_message` 这个字段原本是 B 站语义（「可解析 03:00 / 全长 10:00」），挂在「时长：」标签下天经地义；YouTube 塞进去的却是「为什么拿不到流」，于是文本消息里会出现「时长：被 YouTube 机器人验证挡下，仅展示封面与信息」
- 现在按有没有可解析时长分流：有就仍用「时长：」，没有则单独输出一行真实时长，限制说明改用中性的「提示：」

### 关于门禁本身的结论（不是插件 bug）

- 门禁是**按视频**触发且确定性的：同一台机器同一秒，热门老视频正常出流，被标记的视频连试 3 轮全部 `LOGIN_REQUIRED`。所以重试、换 UA、换客户端版本号都没有意义
- 累计穷举了 21 个 Innertube 客户端，**没有任何一个能在门禁下拿到视频流**。可行解只有两个：在 `youtube.cookie` 填入有效登录 Cookie（README 有 5 步获取流程），或给 `proxy.youtube` 换一个住宅 / 家宽出口（机房 IP 极易被要求人机验证）
- 插件的定位因此调整为：拿不到流时**尽可能把信息卡片做完整**，而不是退化成一句错误提示

### 测试

- 新增 16 个用例：门禁版 `next` 响应的点赞 / 播放量提取（含「误读不喜欢按钮」「压缩文案 vs 精确整数」等回归点）、元数据兜底客户端的成功 / 无字段 / 抛错 / 已在主链则跳过四条路径、限制说明的标签归属
- 全量 319 passed + 72 subtests

## v1.8.2 (2026-08-29)

### 新增两套仿站皮肤：X（推特）与 YouTube

- 此前所有社交平台都只能借用「哔哩哔哩」皮肤的基调，X 的推文顶着 B 站的配色与字重。现在 X 与 YouTube 各有一套完整皮肤（专属深浅两版配色、accent、字重与圆角），不是换色，而是从调色板到区块几何独立成套
- **X 皮肤**按手机端推文详情页重做：`@handle` 灰色副行 + 蓝色对勾认证、时间行改成原生语序「时间 · N 查看」（浏览量不再塞进页签）、页签条退化成一条发丝分隔线、封面不压播放量角标、五枚等距铺满内容宽度的操作栏（回复 / 转推 / 粉色实心喜欢 / 书签 / 分享）、每条回复正文下带一行迷你操作图标、回复区标题改成「回复 / 相关」
- **YouTube 皮肤**按手机端观看页重做：红色下划线页签、`#3EA6FF` 链接蓝、药丸操作栏、原生措辞「评论 / 排序依据 / 添加评论…」
- 操作栏新增第四种画法 `spread`：等距铺满整行内容宽度，用于 X
- 皮肤总数 6 → 8，版式组合 48 → 64

### 皮肤可以「跟随平台」

- 「卡片风格」新增取值 **跟随平台**：B 站链接自动用哔哩哔哩皮肤、X 用 X 皮肤、YouTube 用 YouTube 皮肤，未收录的平台退回极光。此前家具能跟着平台换，皮肤基调却不能，跨站时仍有割裂感
- 只要用户明确选了某套皮肤，插件就绝不擅自替换；自动挑选只在「跟随平台」这一个取值下发生
- 旧皮肤名 `推特卡片` / `油管卡片` 会自动迁移到 X / YouTube

### YouTube 解析失败不再无声无息

- **修复「视频没解析出来但后台日志一片安静」**：取不到可下载流时只在卡片上写一句「仅展示封面与信息」，日志级别却是 DEBUG，排查时看不到任何线索。现在会打一条 WARNING，一行写清尝试过的客户端链、当次登录态（匿名 / Cookie 已鉴权 / Cookie 已失效）、代理是否启用、门禁状态码与 `playabilityStatus`，以及对应处理建议
- **新增 Cookie 失效检测**：从 Innertube 响应里识别 `loggedOut` 标志（兼容 bool / 字符串 / 嵌套三种形态），确认 Cookie 掉登录态后除 WARNING 外还会私聊管理员一条带 4 步重取指引的提醒，带冷却避免刷屏。新增配置「Cookie 失效时私聊管理员」（默认开）与「Cookie 失效提醒冷却」（默认 120 分钟）
- 说明：被机器人门禁挡下**本身不是插件 bug**，是 YouTube 按视频触发的风控。插件能做的是把原因说清楚，并提示填 Cookie 或走住宅代理

### 修复与优化

- **`repost`（转发）图标重画**：原先在 20px 下两条箭头相交糊成一个方块，改为互不相交的上下双向箭头
- 新增 `heart`（实心心形）与 `check`（认证对勾）两种几何图标
- README 补充「皮肤跟随平台」小节、两套新皮肤的图廊与设计语言说明，以及「Cookie 获取与保鲜」章节（无痕窗口 → 同标签页跳 `robots.txt` → 本地导出 → 直接关窗口而不点登出）

## v1.8.1 (2026-08-29)

### 卡片家具跟随平台，不再张冠李戴

- 此前「哔哩哔哩」皮肤把 B 站手机端的界面家具无条件套到所有平台上，于是 YouTube 卡片会顶着一个取不到数的空「赞和转发」页签，底部还蹲着一个「点我发评论」输入框——皮肤是对的，家具是错的。现在把家具从皮肤里拆出来，按链接所属平台换件
- 页签条：次级项改为按平台声明（B 站系「赞和转发」= 点赞+转发之和、X 用「查看」= 浏览量、YouTube 与 Pixiv 没有次级项）。平台没有这个概念、或者数值一个都取不到时整枚隐藏，不再留光标签
- 底部操作栏新增三种画法：`stack`（图标在上数字在下，B 站/抖音系原版样式）、`pill`（圆角药丸包住图标与文字，YouTube 样式）、`inline`（图标数字同行不加底色，X/Pixiv 样式）。三者共用「右对齐 + 给左侧输入药丸让位」的同一套几何，宽度不够时优先保住操作栏
- 操作栏动作集与「无数值时是否隐藏」也按平台定义：YouTube 是 👍/👎/分享/保存（后两个没有数值，固定显示文字），X 是回复/转推/喜欢/书签，B 站系仍是转发/评论/收藏/点赞
- 评论输入框占位文字跟着平台走：`点我发评论` / `添加评论…` / `发布你的回复` / `说点什么…` / `留下评论…`；平台不给占位文案时整个输入框不画
- 卡片角落署名里的 YouTube 链接会折成短链形式（`youtube.com/watch?v=XXX` → `youtu.be/XXX`），省出的横向空间留给正文

### 修复

- **YouTube 热评点赞数不再是假精度**：之前几条热评的赞数经常一模一样（比如全是 `1100`），因为把压缩写法 `1.1K` 反解成了 `1100`。现在只有无障碍文案给出完整数字（`1,100 likes`）时才用精确值，否则把 `1.1K` 原样透出，不再编造末位
- **YouTube 评论时间本地化**：`3 days ago` 之类的英文相对时间会被翻成 `3天前`，不再和后面的中文「回复」中英混排；同时覆盖 `1 month ago (edited)` → `1个月前（已编辑）`、`Streamed 2 weeks ago` → `直播于2周前`、`just now` → `刚刚`、`Premiered Aug 22, 2026` → `2026-08-22`，识别不了的原样保留
- **发布时间不再凭空补 `00:00`**：只有日期没有时刻的时间戳，卡片上就只显示日期
- **评论头像与作者头像变清晰**：Google 头像直链的尺寸参数会被抬到 176px（`=s48` / `=w48-h48` → `=s176`），此前拿的是 48px 缩略图，放到卡片上必然糊
- **圆头像与圆角面板边缘不再发糙**：圆形裁切与圆角面板改为 4 倍超采样后 LANCZOS 缩回，锯齿肉眼可见地消失；直角面板与超大面板走原来的快路径，不影响渲染耗时

## v1.8.0 (2026-08-28)

### 新增平台：YouTube

- 支持 `youtube.com/watch`、`youtu.be`、`/shorts/`、`/live/`、`/embed/`、`/v/` 以及 `music.youtube.com`，同时解包分享用的 `/attribution_link?u=...` 嵌套链接
- 走 YouTube 官方 Innertube 接口，**不依赖 yt-dlp、不依赖第三方镜像站**。整条链路是三层降级：
  - 元数据层：`oembed` 与 Innertube `player` 并发请求，两边互相补字段；都失败时兜底抓 watch 页面内嵌的 `ytInitialPlayerResponse`
  - 媒体层：从 `streamingData` 挑流，优先级 dash 分离流 → progressive 单文件 → HLS → 仅视频轨
  - 增强层：`next` 端点补作者头像、点赞数、评论数与热评
- Innertube 客户端默认只跑 `ios,android_vr`。对 11 个客户端逐一实测后确认：**匿名状态下只有这两个会真正返回可直连的 `adaptiveFormats`**（最高 2160P、无签名挑战），`tv` / `mweb` / `web` / `web_creator` / `ios_music` / `mediaconnect` 等一律 `UNPLAYABLE` 或 `LOGIN_REQUIRED`，一条流都拿不到，放进默认链只会白烧时间预算。顺序仍可在配置里调整
- **Cookie 鉴权按 Innertube 的真实要求实现**：只发 `Cookie` 头是无效的，服务端会当匿名请求处理。现在会从 Cookie 里取 `SAPISID` / `__Secure-3PAPISID` 算出 `Authorization: SAPISIDHASH <ts>_<sha1(ts SAPISID origin)>`，并补 `X-Origin`、`X-Goog-AuthUser`；Cookie 里找不到可用字段时给出警告并退回匿名。原生移动客户端（IOS / ANDROID_VR）会忽略甚至拒绝鉴权，因此**只对声明支持 Cookie 的客户端**下发登录态
- 配置了 Cookie 后会自动把 `tv`、`web` 这两个支持鉴权的客户端追加到尝试链末尾，不需要手动改客户端顺序
- 下载请求头的 User-Agent 会跟产出该直链的客户端保持一致，否则 googlevideo 会直接 403
- 新增配置节「YouTube 设置」：画质上限、是否允许 dash（需要 ffmpeg）、客户端顺序、单次解析总时间预算、Cookie
- 热评开关（消息输出 → 附加内容：热评 → YouTube）默认开启，兼容新版 `commentEntityPayload` 与旧版 `commentRenderer` 两种评论结构
- 统计行为 `👀 播放 / 👍 点赞 / 💬 评论`，点赞数固定用 `hl=en` 请求以便从无障碍文本里稳定取到精确值（而不是 `1.2万` 这种压缩写法）

### YouTube 的设计取舍

- **单一时间预算**：元数据、媒体流、头像/点赞/热评三层共享一个总预算（默认 45 秒），而不是每层各自超时。此前其他平台踩过的坑是每层 10~20 秒叠起来最坏情况能到分钟级；现在预算用尽就跳过剩余增强步骤，只发已经拿到的信息
- **代理单开关**：`代理 → YouTube` 一个开关同时管解析请求和媒体下载。googlevideo 直链与取流时的出口 IP 绑定，解析走代理、下载走直连必定 403，所以不拆成两个开关
- **不实现签名还原，也不实现 PO token**：带 `signatureCipher` / `cipher` 的流直接跳过。还原签名要下载并执行 YouTube 的播放器 JS，PO token 要跑一整套 BotGuard 虚拟机——两者都得在服务器上执行 YouTube 下发的代码，且随时被上游改动打断，不适合作为一个聊天插件的长期依赖
- **机器人门禁只能退化，不强行绕**：部分视频返回 `LOGIN_REQUIRED`（「Sign in to confirm you are not a bot」），这是按视频触发、机房 IP 上尤其常见。已实测排除的无效手段全部记录在 README 与 `docs/PARSER_METHOD_MEMO.md` 里（换客户端 / 换版本号 / 换 UA / `params=8AEB` / `visitorData` / `bpctr=9999999999&has_verified=1`），避免以后重复踩。真正有效的只有填 Cookie 或换住宅代理出口
- **失败退化而非报错**：会员限定、地区限制、年龄限制、私享、直播中等取不到流的情况，退化成「封面 + 标题 + 作者 + 统计」的卡片，并在卡片上说明原因，而不是抛错让整条消息失败
- 降级链细节写 DEBUG 日志，群里只发结论；正常解析在 INFO 留一行摘要（视频 ID、流类型、客户端、热评数、耗时）

### 修复

- 链接提取的尾部裁剪补到 YouTube：`https://youtu.be/xxxx媒体解析` 这类链接紧贴中文的消息此前会把中文当成视频 ID 的一部分（和 v1.7.3 修的 b23 短链是同一类问题）
- `parse_youtube_identity` 解包 `attribution_link` 时校验 scheme / 用户名 / 密码 / 端口，避免被构造成 SSRF 跳板

## v1.7.3 (2026-08-28)

### 修复

- 链接提取新增尾部裁剪：`https://b23.tv/5AeLOCA媒体解析` 这类「链接紧贴中文」的消息此前会把中文一起当成短链路径，导致 B23 展开时报「服务端未返回重定向」。现在按 RFC3986 白名单裁掉尾部的非法字符与孤立标点，查询串、片段、`~`、`+` 等合法字符一律保留；b23 短链正则也收紧成只吃 `[0-9A-Za-z]`
- 「赞和转发 M」页签条恢复 B 站原版语义：M = 点赞数 + 转发数（例如 817 + 19 = 836）。上一版把它拆成独立的「转发 M」，反而和 B 站客户端不一致；两个数字都取不到时页签只留标签，不再显示 `赞和转发 0`

### 哔哩哔哩皮肤：底部操作栏重画

- 操作栏移到卡片最底部（此前在页脚之上），并按 B 站客户端重绘：左侧是吃满剩余宽度的「点我发评论」输入药丸，右侧是转发 / 评论 / 收藏 / 点赞四组「图标 + 数字」竖排右对齐
- 收藏与点赞用品牌粉实心图标，转发与评论用中性灰；四个数字统一用弱化文本色，避免某一格因为颜色不同显得被选中
- 卡片宽度不足时药丸自动隐藏，只保留右侧四组数字，不会出现药丸与数字重叠
- 转发图标重画：原来的「圆弧 + 两条斜线」在 15 像素下箭头会和弧线脱开，看起来像坏图；现在是一条带弧度的折线尾加实心三角箭头

### 署名与原链接

- 哔哩哔哩皮肤把原链接与署名合并成一行浅灰小字，缩到顶栏右上角（沉浸式布局没有顶栏，则画在主视觉右上角的压暗区上）：底部让给原版操作栏，同时不再用整块底板去承载一条长链接
- 链接在角落里去掉 `https://` / `www.` 前缀并按剩余宽度省略，宽度实在不够时只留署名
- 新增「显示卡片署名」「显示原文链接」两个开关，六套皮肤、四种布局全部生效，且都参与卡片缓存键

### 评论头像

- 取不到评论头像时的占位重做：原来是品牌粉底 + 白色首字母，在一排真实头像里非常跳。现在是随昵称微调明度的中性灰圆盘 + 弱化色首字母；昵称是纯 emoji 之类取不到首字母时，画一个几何绘制的人形剪影
- 补齐真实头像来源：B 站（`member.avatar`）、微博（`avatar_large` / `avatar_hd` / `profile_image_url`）、小红书（`user_info.image` 等）此前解析时就没带回头像字段，现在五个有热评的平台全部覆盖

### 文档

- 重新生成 README 的六套皮肤对照截图

## v1.7.2 (2026-08-28)

### 哔哩哔哩皮肤视觉修正

- 顶栏去掉 bilibili 字标与小电视图标：卡片解析的多数是非 B 站链接，贴一个 B 站 logo 只会让人以为来源错了。改为「返回箭头 + 来源站点名」（例如「‹ 推特」）当作顶栏标题，用常规正文色而非品牌粉
- 顶栏不再显示「图文 / 视频」这类内容类型胶囊：正文与媒体区已经把形态说清楚了，多一个标记只是噪音
- 头像右侧不再挂「UP主」标签：非 B 站来源的作者并不是 UP 主
- 署名水印从左下移到右下、与原链接同处一行，并改用中性灰的常规字重（此前是品牌粉加粗，抢走了正文的视觉重心）
- 底部操作栏改为等分整宽、每格居中：水印移走后原来的右对齐会在左侧留下一片空白

### 修复

- 「评论 N / 赞和转发 M」页签条改为「评论 N」「转发 M」两个独立数字：原来第二个页签在没有转发数时会拿点赞数顶上，两个语义混在一个标签里根本看不出算的是什么。现在评论用评论数、转发用转发数，取不到就留空不编造
- Twitter/X 的阅读量图标从 `👀` 改为 `📈`（渲染为「浏览」）：`👀` 在卡片里映射的是 B 站语义的「播放」，而推特那个数字是曝光量，不是播放次数。B 站自己的播放量不受影响

## v1.7.1 (2026-08-28)

### 修复

- 修复自建 Nitter 被插件自己的 SSRF 防护拦下的问题：下载会话装了"连接前只允许公网地址"的防护，而 Nitter 实例通常跑在 `127.0.0.1` 或内网，v1.7.0 一填就报 `UnsafeMediaURLError: 拒绝访问非公网连接前地址`，热评和统计数字仍然是空的。Nitter 地址来自用户配置、属于显式信任，现在改为单独用一个不受公网限制的临时会话抓取，媒体下载依旧走原来的安全会话，SSRF 防护范围不变
- Nitter 已经拿到结果时不再回落到 X 公开页：X 早就不对未登录访问输出回复数据，原来"只缺回复就再试一次"的逻辑会让没有回复的推文白等满 26 秒的热评预算，把整条解析拖慢十几秒

### 改进

- 热评日志分级更准确：X 页面能取回但整页压根不含回复区（`jsonld=False, comment_node=False`）与 403 同属平台限制，降到 DEBUG + 1 小时冷却的 INFO；只有页面确实带回复数据却解析不出来才继续 WARN。此前每条推文都会刷一屏 WARN

## v1.7.0 (2026-08-28)

### Twitter/X 热评改用 Nitter（重要）

- X 已彻底停止对未登录访问输出带回复数据的服务端渲染页：实测爬虫 UA 要么直接 403，要么拿到 200 但页面里既没有 `application/ld+json` 也没有 `Comment` 节点。此前"换 UA / 换路径重试"的思路无法绕过，v1.6.1 的日志里那串 403 就是这个原因
- 新增配置项「附加内容：热评 → Twitter/X 的 Nitter 实例地址」。填入一个可用的 Nitter（Nitter 是 X 的开源前端，例如自建的 `http://127.0.0.1:8585`）即可恢复热评，支持逗号分隔多个实例依次兜底，缺协议时自动按 `http://` 补全
- 指向 `localhost` / `127.x` / `10.x` / `192.168.x` / `172.16~31.x` 的自建实例一律直连，不再被"解析走代理"的设置带去绕代理
- Nitter 抓取有独立时间预算（单实例 10 秒、整体 18 秒），失败时自动回退到原来的 X 公开页路线，正文/图片/视频解析完全不受影响
- 未配置 Nitter 且热评抓不到时，按 1 小时冷却给一条 INFO 引导，不再只留下一串看不懂的 403

### 新增

- Twitter/X 卡片补上互动统计：从 Nitter 读取喜欢 / 转发 / 评论 / 阅读数并写入 `stats_line`，卡片底部的统计区终于不再是空的
- Twitter/X 头像多一层兜底：FxTwitter 与 Guest GraphQL 都没给头像时，用 Nitter 页面里的头像还原成 `pbs.twimg.com` 直链，并升级到 `_400x400` 尺寸（Nitter 默认只给 73px 的 `_bigger`，放到卡片上偏糊）
- Nitter 的图片代理路径统一还原为原始 CDN 直链，卡片产物里不会残留内网地址

### 修复

- 修复卡片正文出现 ⬚ 方块的问题：推特作者常用 `U+2800`（盲文空白）这类"看不见的占位空白"来撑行距，而中日韩字体普遍不含这些字形，直接渲染就是豆腐块。现在文本清洗阶段会把盲文空白、蒙文元音分隔符、韩文填充符、零宽字符、各种不换行空格统一换成普通空格，并丢弃所有本就没有字形的控制/未分配码位
- 文本清洗顺带按行去掉行尾空白，连续空行压缩为一个空行：作者用占位空白堆出来的大段空隙不会再在卡片里撑出一片空白
- 表情剥离范围补上杂项符号箭头（`U+2B00–U+2BFF`）与装饰符号（`U+1F650–U+1F67F`）两个区段
- Nitter 回复解析会跳过已删除 / 受保护的推文占位块，无论 Nitter 是渲染成独立的 `unavailable-box` 元素还是把状态写进 `timeline-item` 的 class

## v1.6.1 (2026-08-28)

### 修复

- 修复 Twitter/X 热评"双来源兜底"实际无效的问题：`twitter.com` 会 301 到 `x.com`，两条请求其实打的是同一个站点，日志却声称"已请求 2 个来源"；现在只保留 `x.com`，改为按「规范路径 → /i/ 路径」和「多个爬虫 UA」两个真正有差异的维度重试
- 抓热评改用 X 的规范帖子路径 `https://x.com/{作者}/status/{id}`（作者 handle 直接取自消息里的链接），`/i/status/{id}` 降级为回退地址：`/i/` 只是重定向入口，服务端渲染对它的支持并不稳定
- 平台明确拒绝（HTTP 401/403/404/429）不再刷 WARN：这是 X 关掉了未登录访问，不是插件故障。详情降到 DEBUG，另按 15 分钟冷却给一条 INFO 说明；网络不通、超时、页面结构变化等真实故障仍然 WARN

### 优化

- 热评抓取加上时间预算：单次请求 12 秒、整体 26 秒上限，超出即放弃剩余来源，避免代理不通时热评把正文解析一起拖慢
- 失败重试不再空转：传输层失败（连不上/超时）会跳过同一代理下的其余地址与 UA；页面已成功取回则不再换 UA 重复请求同一地址
- 热评失败日志附带命中的 UA 标签与是否经代理，便于定位是风控还是网络问题

## v1.6.0 (2026-08-27)

### 哔哩哔哩皮肤重做为动态详情页

- 哔哩哔哩皮肤按 B 站手机端动态详情页重排：返回箭头 + 小电视字标 + 内容类型胶囊 + 更多按钮的顶栏、带蓝色认证角标的大圆头像与右侧「UP主」标签、话题与 @ 提及着蓝色的富文本正文、IP 属地说明行、「评论 N / 赞和转发 M」页签条、带排序入口的热门评论区，以及转发/评论/收藏/点赞四组图标的底部操作栏
- 九宫格改为固定三列的等比方格：末行不足三张时不再横向拉伸，与真实 B 站的相册排布一致
- 统计数字不再重复出现：播放与弹幕留在 IP 说明行，点赞/投币/收藏/转发/评论只出现在底部操作栏

### 新增

- 热门评论头像现在对全部六套皮肤生效：评论头像随卡片素材一起并发下载，绘制为圆形头像；平台没有返回头像时留白，不再只有哔哩哔哩皮肤能显示
- B 站图文动态（opus / t.bilibili.com）补齐互动数据：从 `modules.module_stat` 读取点赞、评论、转发，卡片底部操作栏不再只剩图标没有数字

### 修复

- 修复 Twitter/X 公开回复长期抓不到且无任何报错的问题：改为优先解析页面内嵌的 JSON-LD 结构化数据（比正则匹配内联 JS 稳定得多），使用爬虫 UA 请求公开页，并同时尝试 `x.com` 与 `twitter.com`；全部来源失败时逐条记录原因，不再静默返回空列表
- 修复 Twitter/X 与微博正文里的 HTML 实体不还原的问题：`(&gt;_&lt;)` 之类的表情现在会正确显示为 `(>_<)`；Twitter 按 `display_text_range` 截取之后才反转义，避免偏移错位
- 修复账簿式统计区（报章 / 夜曲皮肤）在杂志布局的窄侧栏里数值整体消失的问题：单元格宽度不足时改为自动折成多行多列，而不是把数值省略成空串
- 修复展陈等皮肤的评论区大引号压住评论头像的问题：头像缩进改为按引号实际宽度计算
- 固定宽高比的网格不再被布局的媒体缩放系数压扁：信息流布局下哔哩哔哩九宫格曾被压成 227×200 的矩形，现在恒为正方形

### 优化

- README 的六套皮肤示意图全部重新生成，包含评论头像与新的哔哩哔哩版式
- 新增测试覆盖九宫格等比、评论页签、热评标题与「时间 + 回复」元信息、IP 说明行的指标白名单、窄栏账簿统计的数值完整性，以及 B 站 `module_stat` 统计行提取

## v1.5.0 (2026-08-27)

### 卡片视觉系统重写

- 卡片渲染整体重建为 `nova_core/card` 设计系统（调色、度量、排版、绘制原语、数据模型、主题、区块、引擎分层），旧的单文件渲染器缩减为兼容入口
- 皮肤重做为六套完整设计语言：极光（渐变玻璃）、报章（纸纹报印、首字下沉）、测控（栅格数据面板）、展陈（画廊装裱、编号图注）、夜曲（午夜霓虹）、哔哩哔哩（还原 B 站视频卡的小电视标志、品牌粉、UP 主行、蓝色链接与图标统计）
- 深浅配色与版式布局现在对所有皮肤都真实生效：每套皮肤各有独立的深浅两版配色，四种布局（标准/杂志/沉浸式/信息流）改变区块的空间组织方式，皮肤 × 深浅 × 布局共 48 种组合
- 卡片先测量再绘制，彻底移除硬编码区块高度：空的正文、引用、统计或评论区不再留下空白面板；热评并入主排版流程，不再拼接第二张画布
- 多图排布改为均衡分行（5 张排成 2+3、7 张排成 2+2+3），末行不再留下空洞；仅主视觉使用毛玻璃衬底补幅，网格缩略图统一裁切填满
- 全部文字颜色经过对比度校正，修复深色“展陈”皮肤正文几乎不可见的问题；图标改为几何绘制，不再依赖系统 emoji 字体
- 页脚长链接按字符折行（最多 3 行）并让底板随行数自动变高，不再为了宽度截断查询参数或片段
- 旧皮肤名（Nova 原生 / 编辑室 / 信号终端 / 海报档案 / 霓虹夜景 / B站动态）自动迁移到新皮肤，无需手动改配置

### 修复

- `initialize` 改为异步生命周期钩子，后台缓存清理任务不再在没有运行事件循环的 `__init__` 阶段创建
- 修复卡片并发渲染时用下标回写元数据可能错位或越界的问题，改为渲染副本与原元数据严格配对，并记录失败任务的链接与原因
- `aiohttp.ClientSession` 构造失败时补上 connector 关闭，避免连接器泄漏；插件卸载时关闭翻译复用的 HTTP 会话
- 分片下载中 `CancelledError` 等非 `Exception` 异常不再被当成普通分片失败吞掉，会先清理临时文件再向上传播
- `os.replace`、`os.makedirs` 等阻塞文件操作移入线程池，不再阻塞事件循环
- 下载目录创建后立即补写缓存归属标记，避免空目录清理逻辑摘掉在用目录的标记
- ZIP 工作目录补写过期时间，`cleanup_expired_zip_workspaces` 不再因缺少过期标记而永久跳过回收
- HTTP 状态码识别不再把 `120.5MB` 之类的数字误判为状态码，需要 status/http/状态码等上下文关键字
- 直播判定不再把 `from=live.share` 这类 tracking 参数当成跳转链接误判为直播
- QQ 卡片链接提取支持 `data` 已被适配器解析成 dict 的情况
- 解析记录只在新增或裁剪过期记录时落盘，命中限流不再触发整文件重写；序列化改为一次性写入

### 优化

- 元数据翻译按批并发（上限 3），单批失败只丢弃该批并保留其余结果，日志记录失败批次数
- LLM 客户端复用同一个 `ClientSession`，并对 408/429/5xx 增加指数退避重试，支持 `Retry-After` 的秒数与 HTTP-date 两种格式
- 文件 Token 注册改为并发执行，不再逐个串行等待远端服务
- 普通视频预检改为并发进行，任一失败即取消其余预检
- 移除无人调用的 `batch_download_videos` 与 `process_gather_results` 等死代码，以及未被消费的 `converted_to_png` 字段
- 补全类型注解，`pillow` 依赖改为固定范围 `>=10,<13`
- 新增卡片测试覆盖 48 种组合的渲染、空区块折叠、长链接完整性、多图分行与对比度

## v1.4.0 (2026-08-24)

- 重做 B站动态皮肤：按动态页的阅读顺序排列作者、时间、正文、多图、互动数据、原始链接和热门评论
- 移除卡片右上角固定的“哔哩哔哩风格”等皮肤说明文字；可配置的右下角解析署名保持不变
- 修复 Twitter/X 等多图内容只突出首图、后续图片过小的问题；双图会等宽展示，多图会按各皮肤自己的结构编排
- 编辑室新增补充画面联系表，信号终端新增编号媒体窗，海报档案新增档案接触表，霓虹夜景新增次级视觉窗
- Nova 原生沉浸式布局补齐后续图片网格；B站动态皮肤最多以 3 x 2 网格展示六张图片
- 新增“卡片单条热评字数上限”，范围 `60-600`、默认 `180`；超长评论先截断，保留内容再完整换行，避免固定行数裁切
- 卡片缓存键现在包含全部媒体地址和热评字数上限，修改图片集合或评论限制后不会错误复用旧卡片
- 优化中英混排换行，`HAL`、作者 ID 等短 ASCII 片段会整体移到下一行，超长链接仍能按字符完整折行
- 增加双图占比、六图覆盖、固定皮肤文字移除、热评截断与完整换行回归测试

## v1.3.1 (2026-08-24)

- Twitter/X 普通推文不再生成“作者名(@ID) 的推文”占位标题，普通文本会直接从作者和发布时间开始展示
- 没有真实标题的社交内容会在五套高级皮肤中把正文提升为主内容，且不会把正文重复绘制两次
- 信号终端在纯图片推文恰好包含两张图时使用两个完整媒体窗，第二张图不再缩成普通缩略图
- 明确高级皮肤使用独立固定配色，不受“卡片主题”和“卡片布局”影响；缓存键也不再因这两个无效配置重复生成文件
- 新增高级皮肤标题占位、信号终端双图和主题隔离回归测试

## v1.3.0 (2026-08-24)

- 新增“霓虹夜景”和“哔哩哔哩风格”两套高级卡片；两者拥有独立作者区、媒体区、正文区、数据栏、链接页脚和热评视觉
- 信号终端重做为绿青双色实时信号台，加入作者头像与身份面板，移除 `//`、`LINK://` 等刻意终端化文案
- 海报档案重做为装裱海报、目录轨、裁切标、档案章、纸质说明卡和来源记录带组成的档案结构
- 高级皮肤与 Nova 原生四种布局均完整保留原始链接，不再删除协议，也不再逐字符截断查询参数或片段
- 所有高级皮肤均绘制作者头像；头像不可用时继续使用首字母占位，不阻断卡片渲染
- 增加五套高级皮肤在 `520`、`720`、`800` 宽度下的渲染、头像和完整链接回归测试

## v1.2.1 (2026-08-24)

- 部分消息节点发送失败时不再向 QQ 会话发送“其余内容已发送”提示，失败详情只记录到后台日志
- 新增“在普通文本中显示热评”开关；关闭后正文仍可按卡片模式发送，热评只保留在卡片中
- 补充部分发送失败静默处理、热评文本开关和配置解析回归测试

## v1.2.0 (2026-08-24)

- 重新梳理卡片与翻译配置：`卡片模式` 统一决定卡片和普通文本如何发送，`译文应用范围` 只决定哪些展示内容使用译文
- 修复“卡片+文本同条发送”实际分开发送的问题；现在首张卡片与首段普通文本使用同一个 AstrBot 消息链
- 修复当前 AstrBot SDK 下合并转发 `Node.uin` 被传入整数而触发校验失败的问题
- 翻译后的标题、正文和热评直接合并到展示元数据，不再额外发送独立的“翻译结果”消息
- 新增可选“热评合并到卡片”，各皮肤最多展示前 5 条，作者、ID、点赞和时间信息会保留
- Twitter/X 新增无需登录的公开回复提取；正文与回复会一起进入翻译流程，页面没有公开回复或结构变化时自动跳过
- 小黑盒 BBS 帖子新增热评获取，并支持只有正文、没有媒体的帖子；游戏详情页不会伪造评论
- 小黑盒设备签名把 8 字节单密钥 TripleDES 等价扩展为 24 字节，兼容新版 `cryptography` 的弃用计划
- 编辑室、信号终端和海报档案加强长作者名、ID 与完整链接排版；移除高级皮肤中不必要的固定 Nova 标头
- 重做信号终端：荧光半透明绿色渐变、背景网格、扫描线、媒体视窗和动态遥测区域
- 评论卡片区域按实际多行内容动态计算高度，修复长用户名或评论可能裁切的问题
- 替换插件头像为用户提供的 Sister Alice 图片
- 增加消息模式、翻译评论、Twitter/X 公开回复、小黑盒热评和多尺寸卡片回归测试

## v1.1.0 (2026-08-24)

- 新增三套独立高级卡片皮肤：编辑室、信号终端、海报档案；每套均使用独立画布结构和信息编排
- 翻译结果现在可直接作用于卡片标题和正文，不再只发送独立译文文本
- 新增翻译输出模式：仅作用于卡片、卡片和文本都发送
- 卡片右下角署名改为可配置，默认 `Nova解析`，可自定义为 `Alice解析` 等文字
- 默认手动触发关键词新增 `媒体解析`
- 卡片缓存键加入皮肤、署名和视觉版本，避免错误复用旧卡片
- 替换带固定角色和文案的旧兜底封面，改用原创无文字 Nova 抽象纹理

## v1.0.0 (2026-08-22)

- ✨ 独立命名：插件 ID、内部包名、日志、缓存标记和 ZIP 归档目录改为 Nova 专属命名空间
- 🐛 修复 Twitter/X 解析中未定义 `avatar_url` 导致的 `NameError`
- 🛡️ 为统一元数据补充头像字段兜底，增强 Twitter/X 视频候选项容错
- 🔢 保留 Twitter/X 链接在原消息中的出现顺序
- 📚 重写 README，补充安装、配置、缓存、代理、隐私、上游致谢和问题反馈说明

## 上游历史：v7.0.1 (2026-08-19)

- ✨ 小红书无水印解析增强：图集图片改用无水印原图直链（sns-img-hw），解析补充作者头像，渲染缓存按内容自动失效
- 🐛 修复：日志 logger 去掉 getChild 包装，避免 plugin_tag 丢失导致日志崩溃

## 上游历史：v7.0.0 (2026-08-17)

- 🚀 同步主仓库 v7.0.0：B站 Cookie 高画质与管理员协助扫码登录、媒体中转模式、下载器重写（安全连接、图片格式转换、HLS 正确封装）、全平台解析器重写、配置管理器重写等全部更新
